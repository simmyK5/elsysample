import type { EaseClientOptions, IncidentPayload, StreamChunk } from "./types";
export declare class EaseClient {
    private baseUrl;
    private apiKey;
    private timeoutMs;
    private wsUrl?;
    private ws?;
    constructor(opts: EaseClientOptions);
    private fetchJson;
    reportIncident(payload: IncidentPayload): Promise<any>;
    startAudioStream(): Promise<void>;
    sendAudioChunk(chunk: StreamChunk): void;
    stopAudioStream(): void;
    getQuota(): Promise<any>;
}
