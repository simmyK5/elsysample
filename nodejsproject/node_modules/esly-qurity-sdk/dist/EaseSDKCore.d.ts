export interface EaseSDKCoreOptions {
    apiKey: string;
    baseUrl: string;
    wsUrl?: string;
    debug?: boolean;
}
export default class EaseSDKCore {
    private apiKey;
    private baseUrl;
    private wsUrl;
    private debug;
    private socket;
    private streamId;
    private connected;
    constructor({ apiKey, baseUrl, wsUrl, debug }: EaseSDKCoreOptions);
    log(...args: any[]): void;
    reportIncident({ audio, location, metadata, }: {
        audio?: Blob;
        location: string;
        metadata?: Record<string, any>;
    }): Promise<any>;
    connectStream(streamId: string): Promise<boolean>;
    sendAudioChunk({ audioBase64, seq, final, metadata, }: {
        audioBase64: string;
        seq?: number;
        final?: boolean;
        metadata?: Record<string, any>;
    }): void;
    endStream(): Promise<void>;
}
