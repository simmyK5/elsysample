import type { StreamChunk } from "./types";
export declare class EaseWebSocket {
    private ws?;
    private wsConnected;
    private wsUrl;
    constructor(wsUrl: string);
    connect(onOpen?: () => void, onError?: (err: any) => void, onClose?: () => void): void;
    sendChunk(chunk: StreamChunk): void;
    close(): void;
}
