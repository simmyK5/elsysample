"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.WebSocketImpl = exports.fetchFn = void 0;
let fetchFn;
let WebSocketImpl;
if (typeof window !== "undefined" && window.fetch && window.WebSocket) {
    exports.fetchFn = fetchFn = window.fetch.bind(window);
    exports.WebSocketImpl = WebSocketImpl = window.WebSocket;
}
else {
    // Node.js
    // make TypeScript happy with import
    const nodeFetch = require("node-fetch");
    const wsModule = require("isomorphic-ws");
    exports.fetchFn = fetchFn = nodeFetch;
    exports.WebSocketImpl = WebSocketImpl = wsModule;
}
