"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const EaseSDKCore_1 = __importDefault(require("./src/EaseSDKCore"));
const isBrowser = typeof window !== "undefined" && typeof window.document !== "undefined";
const isReactNative = typeof navigator !== "undefined" &&
    navigator.product === "ReactNative";
const isNode = !isBrowser && !isReactNative;
class EslyQuritySDK extends EaseSDKCore_1.default {
    constructor(config) {
        super(config);
        if (isReactNative) {
            // React Native specific hooks
        }
        else if (isBrowser) {
            // Browser hooks
        }
        else if (isNode) {
            // Node hooks
        }
    }
}
exports.default = EslyQuritySDK;
