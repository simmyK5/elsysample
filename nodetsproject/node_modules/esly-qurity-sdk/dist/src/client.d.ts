import type { EaseClientOptions, StreamChunk } from "./types";
export declare class EaseClient {
    private baseUrl;
    private apiKey;
    private email;
    private timeoutMs;
    private wsUrl?;
    private ws?;
    constructor(opts: EaseClientOptions);
    private fetchJson;
    reportIncident({ audio }: {
        audio?: Blob;
    }): Promise<any>;
    startAudioStream(): Promise<void>;
    sendAudioChunk(chunk: StreamChunk): void;
    stopAudioStream(): void;
    getQuota(): Promise<any>;
}
