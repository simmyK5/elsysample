export interface EaseSDKCoreOptions {
    apiKey: string;
    email: string;
    baseUrl: string;
    wsUrl?: string;
    debug?: boolean;
}
export default class EaseSDKCore {
    private apiKey;
    private email;
    private baseUrl;
    private wsUrl;
    private debug;
    private socket;
    private streamId;
    private connected;
    constructor({ apiKey, email, baseUrl, wsUrl, debug }: EaseSDKCoreOptions);
    reportIncident({ audio }: {
        audio?: Blob;
    }): Promise<any>;
    connectStream(streamId: string): Promise<boolean>;
    sendAudioChunk({ audioBase64, seq, final, }: {
        audioBase64: string;
        seq?: number;
        final?: boolean;
    }): void;
    endStream(): void;
}
